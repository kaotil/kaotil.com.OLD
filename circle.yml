machine:
  timezone: Asia/Tokyo
#dependencies:
#  pre:
#    - sudo pip install awscli
test:
  override:
    - echo Hello World
deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh
