machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.1.3
dependencies:
  pre:
    - sudo /usr/local/bin/pip install awscli
deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh
